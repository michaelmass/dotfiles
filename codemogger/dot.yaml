windows:
  installs:
    cmd: pnpm add -g codemogger --config.node-linker=hoisted
    depends:
      - pnpm

linux|darwin:
  installs:
    cmd: pnpm add -g codemogger --config.node-linker=hoisted
    depends:
      - pnpm
