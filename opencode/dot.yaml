windows:
  installs:
    cmd: pnpm i -g opencode-ai
    depends:
      - pnpm
  links:
    opencode.json: ~/.config/opencode/opencode.json

linux|darwin:
  installs:
    cmd: pnpm i -g opencode-ai
    depends:
      - pnpm
  links:
    opencode.json: ~/.config/opencode/opencode.json
