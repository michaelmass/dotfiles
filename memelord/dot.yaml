windows:
  installs:
    cmd: pnpm add -g memelord --config.node-linker=hoisted
    depends:
      - pnpm

linux|darwin:
  installs:
    cmd: pnpm add -g memelord --config.node-linker=hoisted
    depends:
      - pnpm
